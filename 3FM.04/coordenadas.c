#include <stdio.h>
#include <stdlib.h>
#include <time.h>
void insertionSort(int * V, int n);
void exibirVetor
int main(){
    int v[] = {1, 9, 7, 8, 5, 2};
    for(int i = 0; i < v; i++){
        int menor = 0;
        if(v[i] < menor){
            return v[i];
            printf("%d", v[i]);
        }else{
            return menor;
        }
    }
    for(int i = 0; i < v; i++){
        int maior = 0;
        if(v[i] > maior){
            return v[i];
            printf("%d", v[i]);
        }else{
            return maior;
        }
    }
    return 0;
}
void InsertionSort(int * V, int n){`
}